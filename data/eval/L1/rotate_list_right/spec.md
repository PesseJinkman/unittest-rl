Write a function rotate_list_right(items, k) that returns a new list with the elements of items rotated to the right by k positions. Do not modify the input list. If items is empty, return an empty list. k may be zero, larger than the list length, or negative. Negative k means rotate left by -k positions. Use modular behavior so rotating by the list length returns an equivalent list.
