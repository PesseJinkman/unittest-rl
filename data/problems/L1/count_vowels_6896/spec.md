Write a function count_vowels(text) that returns the number of English vowels in a string. Count only the letters a, e, i, o, u, and treat uppercase and lowercase the same. Any other character, including y, spaces, digits, punctuation, and accented letters, does not count. Return 0 for an empty string. The function should not modify the input and should return a non-negative integer.
