Write a function longest_common_prefix(words) that returns the longest starting substring shared by every string in the list. If the list is empty, return "". If any string is empty, the result is "" unless the list has just one empty string, in which case it is still "". Comparison is case-sensitive. For a list with one string, return that string unchanged. Do not modify the input list.
